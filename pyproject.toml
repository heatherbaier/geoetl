[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "geoetl"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
geoetl = "geoetl.cli:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["geoetl*"]